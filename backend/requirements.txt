Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-CORS==4.0.1
bcrypt==4.2.0
pyotp==2.9.0
python-dotenv==1.0.1
requests==2.32.3
psycopg2-binary==2.9.9
pytest==8.3.3
gunicorn==22.0.0

# --- Optional: real malware scanning (file-security module) ---
# The default "demo" scanner (EICAR + heuristics) needs nothing extra.
# For REAL signature scanning, run a ClamAV daemon and install the client,
# then set SCANNER_MODE=clamav :
#   clamd==1.0.2
# For stronger content-based MIME detection (falls back to a safe sniffer):
#   python-magic==0.4.27
